{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug server",
      "type": "node",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "program": "${workspaceFolder}/node_modules/.bin/nx",
      "args": ["serve", "server"],
    },
    {
      "name": "Debug game",
      "type": "chrome",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "program": "${workspaceFolder}/node_modules/.bin/nx",
      "args": ["serve", "game"],
    }
  ]
}
